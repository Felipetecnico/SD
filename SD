<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Clínica Veterinária - Atendimento ao Tutor</title>
</head>
<body>
  <script>
    // 1. Boas-vindas
    let nomeTutor = prompt("Olá! Seja bem-vindo à nossa clínica veterinária. Qual é o seu nome?");
    alert(`👋 Olá, ${nomeTutor}! É um prazer atender você e seu pet!`);

    // 2. Verificação de idade do tutor
    let idadeTutor = parseInt(prompt("Por favor, informe sua idade:"));
    if (idadeTutor >= 18) {
      alert("✅ Você é maior de idade e está autorizado a prosseguir com o atendimento.");
    } else {
      alert("⚠️ Você é menor de idade. É necessário um responsável maior de idade para autorizar o atendimento.");
    }

    // 3. Número da sorte - par ou ímpar
    let numeroSorte = parseInt(prompt("Por curiosidade... Qual é seu número da sorte?"));
    if (numeroSorte % 2 === 0) {
      alert("🔢 Seu número da sorte é PAR!");
    } else {
      alert("🔢 Seu número da sorte é ÍMPAR!");
    }

    // 4. Mini calculadora
    let operacao = prompt("Vamos simular um valor de serviço. Escolha a operação (+, -, *, /):");
    let num1 = parseFloat(prompt("Informe o primeiro valor:"));
    let num2 = parseFloat(prompt("Informe o segundo valor:"));
    let resultado;

    switch (operacao) {
      case '+':
        resultado = num1 + num2;
        break;
      case '-':
        resultado = num1 - num2;
        break;
      case '*':
        resultado = num1 * num2;
        break;
      case '/':
        if (num2 === 0) {
          alert("🚫 Erro: Divisão por zero não é permitida.");
        } else {
          resultado = num1 / num2;
        }
        break;
      default:
        alert("⚠️ Operação inválida!");
    }

    if (resultado !== undefined) {
      alert(`📊 Resultado da operação: ${resultado.toFixed(2)}`);
    }

    // 5. Estação do ano
    let mes = prompt("Digite o mês atual para te darmos uma dica sazonal:").toLowerCase();
    let estacao;

    switch (mes) {
      case 'dezembro':
      case 'janeiro':
      case 'fevereiro':
        estacao = "Verão";
        break;
      case 'março':
      case 'abril':
      case 'maio':
        estacao = "Outono";
        break;
      case 'junho':
      case 'julho':
      case 'agosto':
        estacao = "Inverno";
        break;
      case 'setembro':
      case 'outubro':
      case 'novembro':
        estacao = "Primavera";
        break;
      default:
        estacao = "Mês inválido";
    }

    alert(`📆 Estação do ano: ${estacao}`);

    // 6. Número positivo e contagem de pares
    let numeroPositivo = parseInt(prompt("Informe um número positivo (como a idade do seu pet):"));

    if (numeroPositivo > 0) {
      alert(`✅ Número informado: ${numeroPositivo}`);
      let pares = [];
      let contador = numeroPositivo + 1;
      while (pares.length < 5) {
        if (contador % 2 === 0) {
          pares.push(contador);
        }
        contador++;
      }
      alert(`🔢 Os próximos 5 números pares acima de ${numeroPositivo} são: ${pares.join(", ")}`);
    } else {
      alert("⚠️ Número inválido! Deve ser maior que zero.");
    }

    // 7. Idade do cachorro em anos humanos
    let idadeCao = parseInt(prompt("Quantos anos seu cachorro tem?"));
    function idadeHumana(idade) {
      return idade * 7;
    }
    alert(`🐶 A idade do seu cão em anos humanos é aproximadamente: ${idadeHumana(idadeCao)} anos.`);

    // 8. Cálculo de área de formas geométricas
    let forma = prompt("Vamos fazer um cálculo divertido! Escolha uma forma geométrica: quadrado, triângulo ou círculo").toLowerCase();

    function areaQuadrado(lado) {
      return lado * lado;
    }

    function areaTriangulo(base, altura) {
      return (base * altura) / 2;
    }

    function areaCirculo(raio) {
      return Math.PI * raio * raio;
    }

    switch (forma) {
      case "quadrado":
        let lado = parseFloat(prompt("Informe o lado do quadrado:"));
        alert(`📐 A área do quadrado é: ${areaQuadrado(lado).toFixed(2)}`);
        break;
      case "triangulo":
        let base = parseFloat(prompt("Informe a base do triângulo:"));
        let altura = parseFloat(prompt("Informe a altura do triângulo:"));
        alert(`📐 A área do triângulo é: ${areaTriangulo(base, altura).toFixed(2)}`);
        break;
      case "círculo":
      case "circulo":
        let raio = parseFloat(prompt("Informe o raio do círculo:"));
        alert(`📐 A área do círculo é: ${areaCirculo(raio).toFixed(2)}`);
        break;
      default:
        alert("❌ Forma geométrica inválida.");
    }

    // 9. Encerramento
    alert(`💙 Obrigado pelo seu tempo, ${nomeTutor}! Desejamos saúde e bem-estar ao seu pet. A Clínica Veterinária agradece sua visita!`);
  </script>
</body>
</html>
